/**
 * @fileOverview saludo.js
 * @author Nono Karames
 * @version 1.0.0
 */

const saludar = () => {
  const nombre = 'Nono Karames';
  const mensaje = `Saludos ${nombre}`;
  return mensaje;
};

export default saludar;
