/**
 * @fileOverview funciones.js
 * @author Nono Karames < karames@gmail.com >
 * @version 1.0.0
 */

export const suma = (a, b) => a + b;
export const resta = (a, b) => a - b;
export const multiplicacion = (a, b) => a * b;
export const division = (a, b) => a / b;
